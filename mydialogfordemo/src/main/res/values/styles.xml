<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="CustomDialog" parent="Theme.AppCompat.Dialog" >
        <item name="android:windowNoTitle">true</item>
      <item name="android:windowBackground">@color/MyDialogBackage</item>
        <item name="android:windowAnimationStyle">@style/MyDialogAnimatonStyle</item>
    </style>
    <style name="MyDialogAnimatonStyle">
        <item name="android:windowEnterAnimation">@anim/dialogenter</item>
        <item name="android:windowExitAnimation">@anim/dialogexit</item>
    </style>


</resources>
